[tool.ruff]
target-version = "py310"
line-length = 88
extend-exclude = [".pytest_tmp", ".venv", "data", "models", "reports"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I", "C4"]

[tool.ruff.lint.isort]
known-first-party = ["src", "scripts", "tests"]
